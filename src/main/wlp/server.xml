<server>

    <featureManager>
        <feature>webProfile-7.0</feature>
        <feature>servlet-3.1</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080">
        <tcpOptions soReuseAddr="true"/>
    </httpEndpoint>

    <application name="bikeshop" context-root="/bike-shop" location="${appLocation}" type="war"/>

</server>
